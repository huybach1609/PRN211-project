@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<style>
    .hidden {
        display: none;
    }

    #alertDiv {
        transition: opacity 0.3s ease-in-out, transform 0.3s ease-in-out;
        opacity: 0;
        transform: translateY(-20px);
    }

        #alertDiv.visible {
            opacity: 1;
            transform: translateY(0);
        }
</style>
<div id="alertDiv" role="alert" class="flex w-10/12 m-5 pl-4 py-4 text-base text-white bg-gray-900/70 backdrop-blur-sm rounded-lg font-regular hidden z-[99]">

        <script>
        // Check if ViewBag.Mess is not null or empty
    
        var showMessage = '@(ViewBag.Mess != null && !string.IsNullOrEmpty(ViewBag.Mess))' ;
        

        function showAlert() {
            var alertDiv = document.getElementById('alertDiv');
            alertDiv.style.display = 'flex'; // Show the alert

            // Trigger a reflow to enable the transition
            alertDiv.offsetHeight;

            alertDiv.classList.add('visible');

            setTimeout(function () {
                alertDiv.classList.remove('visible'); // Hide the alert after 3 seconds
            }, 3000);
        }
        // Call showAlert if showMessage is true
        if (showMessage) {
            showAlert();
        }
    </script>
